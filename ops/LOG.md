# יומן האופטימיזציה

רשומה לכל שינוי שנועד להזיז מדד. הפורמט בנספח ו׳ של `ops/COWORK-OPTIMIZE.md`. חדש למעלה.

## 2026-09-06 · A1–A5, B1–B10, F1/F3/F5, G1–G11, H1–H10, I2/I4, D8 · חמש חזיתות במקביל
- מה שונה: כל מה שהתדריך מסמן כ״מותר בלי לשאול״, בחמישה ענפים מקבילים שמוזגו לאחד · PR #98
  - **אמון:** המחולל השעתי לא מכריע יותר לבד (כותב הצעה, ו-`upsertMarkets` מסרב להסדיר בלי מקור מאושר); הגרף מציג את קו האומדן ש-`/about` מבטיח; אזהרת ״מחיר ראשוני״ נבדקת מול מספר עסקאות אמיתי ומופיעה על 32 כרטיסים; שורות מפוברקות סומנו או הוסרו ו-`/activity` הפך ל-noindex; ה-API הציבורי ו-llms.txt מפרסמים מספרים אמיתיים.
  - **מדידה:** חותמת בנייה על כל אירוע; `rapid_seen`/`rapid_session` נותנים מכנה ועומק ריצה לחפיסה; שימור D1/D7 לפי מקור; זמן לתשובה ראשונה; פילוח דפדפן in-app; המשפך סופר את מצב זריז; Vitals לפי מכשיר.
  - **מהירות:** cache ללוח (TTFB סדרתי 96→28ms, ב-8 במקביל 673→157ms); תמונות מועמדים 230KB→46KB; פונט בלי reflow (CLS 0.057→0); שלד תואם (קפיצה 115px→4px); navigationPreload; אינדקס וגיזום ל-drift; דחיסה ל-Caddy.
  - **חשיפה:** og.png 303KB→82KB; קישורים נעקבים בקטגוריה 12→59; ניקוי sitemap ו-404; שיתוף נושא `ref=share` עם כפתור WhatsApp.
  - **בטיחות:** גיבוי לילי למסד (לא היה בכלל); הגבלות קצב על נקודות הכסף; השוואת טוקן בזמן קבוע ותפוגה לעוגייה; המודאל לא חוסם שאלה מקישור ולא נחשב LCP; סכום מעל התקרה נחסם בשדה; שפת פאנל התשובה; בדיקות מכונה לאיכות שאלות (12 שגיאות ו-86 אזהרות על הלוח הנוכחי).
- למה: סעיפים 5.A–5.I בתדריך, לפי סדר העדיפויות שנקבע שם
- מדד: אין מדד יחיד — כל שורה נמדדת בנפרד. הראשונים לבדוק: `paid_touched/paid_sessions`, TTFB p50, LCP p75, `rapid_session` (עומק ריצה), וקיום קובץ גיבוי מאתמול.
- +24h: · +7d: · מסקנה:
- הערות: (1) שתי בדיקות drift נכשלו לפי שעת היום עוד לפני העבודה הזאת — תוקנו. (2) `≈` והחצים בקופי מושכים שתי תת-קבוצות פונט (38KB) בחפיסה; זו החלטת קופי ולא תיקון קוד, לא נגעתי. (3) גיבוי מחוץ לשרת דורש דלי ומפתח מהבעלים.

## 2026-09-06 · #95 · תנועה ממומנת: השאלה של המודעה נענית מעל הקפל, ההרשמה מגיעה לשחקנים, והמשפך נמדד
- מה שונה: דף הנחיתה נפתח בכרטיס חי מעל הקפל (הכפתור הראשון 918px → 490px); הצעה רכה עם Google אחרי 3 תשובות; חומה אחרי 10 סביב ערך התשובות; סיכום ריצה לאורח; חפיסה של 16 כרטיסים לאורח (548KB → 185KB); אירועים `landing`, `guest_answer`, `guest_soft_ask`, `guest_gate`, `guest_redeem`, `login_error`; משפך ממומן ב-`/admin/traffic` ובחבילה (`paid`); gtag נטען מיד בנחיתה ממומנת; מסנן בוטים לא מפיל in-app; אזור זמן Asia/Jerusalem · PR #95 · פרוס ב-21:49 (שעון ישראל)
- למה: 89% מ-70 מבקרי הקמפיין עזבו את `/welcome` בלי לגעת בכלום; 0 הגיעו לחומה; מה שביניהם לא נמדד
- מדד: `paid_touched / paid_sessions` · לפני: ~11% (n=70 מבקרים, יום קמפיין אחד) · יעד: ≥35%
- מדד משני: `paid_deck / paid_sessions` · לפני: לא נמדד · יעד: ≥25%
- +24h: · +7d: · מסקנה:
- הערה: ב-GA4 שני אירועים שינו שם — `welcome_answer` → `guest_answer`, `rapid_guest_redeem` → `guest_redeem`
