# AGENT.md — נוהל העבודה של רוטינת העדכון (עדכון שאלות פעם בשעה)

קובץ זה הוא התדריך העריכתי של **בחירות מרקט**. רוטינת העדכון של צוות המערכת רצה פעם בשעה,
פותחת סשן חדש בריפו הזה, וקוראת את הקובץ הזה כדי לדעת מה לעשות. אותם כללים עריכתיים נמצאים גם ב-
`src/lib/agent/prompt.ts` (למחולל המובנה שרץ מתוך האתר עם Vercel Cron).

**הקובץ הזה אומר מה עושים. הסקיל `.claude/skills/market-questions` אומר איך מחליטים** — איך
מחפשים שאלה, איך מוודאים שהיא ניתנת להכרעה, איך קובעים את `initialProbability`, ואיך בוחרים
את `liquidity`. בכל ריצה שמוסיפה או מתמחרת שאלות — קראו אותו.

## המטרה בכל ריצה

1. **לחקור** את החדשות הפוליטיות בישראל מה־24–72 השעות האחרונות (WebSearch/WebFetch, בעברית ובאנגלית).
2. **להוסיף 2–6 שאלות חדשות** — רק אם הן באמת חדשות, טקטיות, וניתנות להכרעה. אם אין חדשות מעניינות — אל תוסיף כלום. איכות מעל כמות.
3. **להכריע שווקים** שהאירוע שלהם קרה (או שמועד היעד עבר בלי שקרה), עם ראיה ומקור.
4. **לוודא** (`npm run markets:validate` לסכמה, `npm run markets:audit` לבריאות הלוח), **לבצע commit ו-push** של `data/markets.json` (ו-`data/people.json` אם נוספו אנשים).
5. אם מוגדרים `SITE_URL` ו-`ADMIN_TOKEN` בסביבה — **לדחוף מיד** את השינויים ל-API של האתר כדי שלא יחכו ל-deploy.

## סגנון השאלות (רמת החדות הרצויה)

- "האם יאיר גולן יגיש תביעת דיבה נגד שרה נתניהו עד 31.12.2026?"
- "האם ערוץ 14 יפרסם סקר שנותן לליכוד 30 מנדטים ומעלה עד 15.9?"
- "האם בנט ואיזנקוט יודיעו על רשימה משותפת עד 30.9?"
- "האם הכנסת תאשר את חוק הגיוס בקריאה ראשונה עד סוף מושב החורף?"
- "האם 'הדמוקרטים' יעברו את 12 המנדטים בסקר ערוץ 12 הבא?"
- "האם נתניהו יעיד במשפטו ביום שני הקרוב כמתוכנן?"

שאלות טקטיות על **אירוע ספציפי** עם **מועד יעד** ו**מקור מכריע** — לא "מי ינצח בבחירות".

### תמהיל מועדים — חובה בכל ריצה

**לפחות שליש מהשאלות שאתם מוסיפים חייבות להיסגר תוך 24–72 שעות.** אלה השאלות שגורמות לאנשים לחזור לאתר, כי אפשר לשחק בהן ולראות הכרעה מיד. שאר השאלות — 1–6 שבועות.

דוגמאות לשאלות של היום ומחר:
- "האם סקר חדשות 12 של מוצאי שבת ייתן לליכוד 25 מנדטים ומעלה?"
- "האם תוכרז רשימה משותפת חדשה עד יום ראשון ב-20:00?"
- "האם הממשלה תאשר בישיבתה מחר את X?"
- "האם נתניהו יתראיין באולפן חדשות עד מוצאי שבת?"
- "האם בג"ץ ייתן צו ביניים בעתירה נגד Y עד יום שני?"

בשאלות כאלה תמחרו בזהירות: בחלון של 48 שעות רוב האירועים הדרמטיים **לא** קורים, ולכן `initialProbability` נמוך הוא בדרך כלל הנכון. ודאו שה-`resolutionCriteria` אומר במפורש מה קורה אם האירוע לא מתרחש כלל בחלון (כמעט תמיד: "לא").

**הכותרת חייבת לעמוד בפני עצמה:** ב"מצב זריז" (`/rapid`) מוצגות רק `title` ו-`subtitle`, בלי ה-`description`. אם אי אפשר לענות
על השאלה כן/לא בלי לקרוא את הרקע — נסחו אותה מחדש.

## כללים מחייבים לכל שאלה

| שדה | כלל |
|---|---|
| `slug` | kebab-case באנגלית, ייחודי, תיאורי. אין לשנות slug של שוק קיים (הוא המפתח במסד הנתונים). |
| `title` | עברית, מתחיל ב"האם", מסתיים ב"?", כולל מועד יעד. עד 180 תווים. |
| `subtitle` | משפט אחד (אופציונלי) שמסביר את ההקשר. |
| `description` | 2–4 משפטי רקע עובדתיים, עם תאריכים. |
| `resolutionCriteria` | מה נחשב "כן", מה "לא", מי המקור המכריע, ומקרי קצה. חייב להיות חד־משמעי. |
| `category` | אחד מ: `polls`, `coalition`, `parties`, `netanyahu`, `legal`, `media`, `security`, `haredi`, `knesset`, `election-day`, `general`. |
| `people` | מזהים מ-`data/people.json`. הראשון ברשימה מספק את התמונה. אם צריך אדם חדש: הוסיפו אותו ל-`data/people.json` עם `wiki` (שם הערך באנגלית) והריצו `npm run people:fetch`. |
| `closesAt` | ISO עם אזור זמן (`2026-09-15T20:59:59+03:00`) = מועד היעד של השאלה. בין שעתיים ל-120 יום מעכשיו. |
| `initialProbability` | ההערכה הכנה שלך (0.05–0.95). לא 0.5 אוטומטית. שיטת התמחור: `.claude/skills/market-questions/references/pricing.md`. |
| `liquidity` | 2000 אלא אם `npm run markets:liquidity -- --p <ההסתברות>` אומר אחרת. נקבע פעם אחת — `sync.ts` לא מעדכן אותו בשוק קיים. |
| `sources` | 1–4 קישורים **אמיתיים** לכתבות (ynet, הארץ, כאן, N12, ערוץ 13, ערוץ 14, מעריב, ישראל היום, וואלה, TOI, JPost, גלובס). אין להמציא URL — אם אינך בטוח שהקישור קיים, השתמש בעמוד שמצאת בחיפוש. |
| `createdAt` | ISO של רגע היצירה. `createdBy`: `"editorial-routine"`. |
| `status` | `open` בשוק חדש. |

**אסור:** שאלות על מוות/מחלה של אנשים; שאלות שמעודדות אלימות; שאלות לא ניתנות להכרעה; כפילויות של שאלות קיימות (גם בניסוח אחר); שאלות שכבר הוכרעו; שאלות שמציגות טענה עובדתית שנויה במחלוקת כעובדה.

## הכרעת שוק קיים

עדכנו את האובייקט הקיים ב-`data/markets.json` (אל תמחקו אותו):

```json
"status": "resolved",
"resolution": "YES",
"resolutionNote": "ב-12.9 פרסם ערוץ 14 סקר דיירקט פולס שנתן לליכוד 31 מנדטים. מקור: https://...",
"resolvedAt": "2026-09-12T21:30:00+03:00"
```

- הכריעו רק עם ראיה מפורשת ופומבית. אם מועד היעד עבר והאירוע לא קרה — `"resolution": "NO"` עם הסבר.
- שוק שהתברר כלא ניתן להכרעה (למשל השאלה התבססה על הנחה שגויה) — `"status": "cancelled"` + `resolutionNote`. כל הכסף מוחזר.
- לעולם אל תשנו את `initialProbability`, `liquidity` או `slug` של שוק קיים. מותר לתקן ניסוח/מקורות/`closesAt` אם התגלתה טעות.
- הגרף מציג לפני מועד הפתיחה **אומדן מקווקו** חסום ל-±3 נקודות אחוז מהמחיר הרשום (`src/lib/synthetic-history.ts`, תצוגה בלבד).
  זו סיבה נוספת לתמחר את `initialProbability` בכנות: הוא העוגן שהאומדן נבנה סביבו — ואותו גרף מוצג גם על הכרטיס במצב זריז.

## הפורמט של `data/markets.json`

```json
{
  "version": 1,
  "updatedAt": "2026-09-04T12:00:00+03:00",
  "lastUpdateNote": "נוספו 3 שאלות על סקרי סוף השבוע; הוכרע השוק על עדות נתניהו",
  "markets": [ { ...MarketContent } ]
}
```

הסכמה המלאה (zod): `src/lib/content.ts`. `npm run markets:validate` חייב לעבור לפני commit.

## מצב האתר (עודכן 5.9.2026)

באתר כ-344 שאלות פתוחות, מהן כ-45 שנסגרות ב-72 השעות הקרובות. המשמעות לרוטינה:

- **ההכרעות הן העבודה החשובה יותר.** בכל ריצה עברו קודם על השווקים שעבר מועד הסגירה שלהם והכריעו אותם. שוק סגור שלא הוכרע הוא באג גלוי למשתמשים.
- **איכות לפני כמות בהוספה.** יש כבר כיסוי רחב לסקרים, לרשימות, למפלגות, למשפט, לביטחון, לכלכלה ולקואליציה. הוסיפו שאלה חדשה רק אם היא נובעת מחדשות של היממה האחרונה ואין לה מקבילה בקובץ.
- לפני הוספה, חפשו בקובץ מילות מפתח מהשאלה שאתם מתכננים כדי לוודא שאין כפילות.

## צעדי הריצה (checklist)

```bash
git pull --ff-only
npm ci --no-audit --no-fund          # (אם node_modules חסר)
cat data/markets.json                # מה קיים? מה פתוח? מה עבר את closesAt?
npm run markets:audit                # חוב הכרעות, תמהיל מועדים, משמעת אחוזים, כיול
# ... מחקר עם WebSearch/WebFetch ...
# ... עריכת data/markets.json (+ data/people.json במידת הצורך, ואז npm run people:fetch) ...
npm run markets:validate             # סכמה — חייב לעבור
npm run markets:audit                # בריאות הלוח — תקנו כל ✗
npm run history:verify               # רק אם נגעתם ב-src/lib/synthetic-history.ts
git add data/markets.json data/people.json
git commit -m "markets: <תקציר קצר בעברית של מה נוסף/הוכרע>"
git push
# אם יש SITE_URL ו-ADMIN_TOKEN בסביבה:
node -e '
const f=require("./data/markets.json");
fetch(process.env.SITE_URL+"/api/admin/markets",{method:"POST",headers:{"content-type":"application/json",authorization:"Bearer "+process.env.ADMIN_TOKEN},body:JSON.stringify({markets:f.markets,note:f.lastUpdateNote,source:"routine"})}).then(r=>r.json()).then(j=>console.log(JSON.stringify(j)))'
```

אם אין שינויים (לא נוספו שאלות ולא הוכרעו שווקים) — אל תבצעו commit ריק. סיימו עם סיכום קצר.

## עובדות רקע (עדכנו כאן כשמשהו משתנה) — נכון ל-4.9.2026

- **לוח זמנים**: הכנסת ה-25 פוזרה ב-17.7.2026. הגשת הרשימות הסופיות לוועדת הבחירות: 9–10.9.2026. הודעות לבוחר: 6.10. תעמולה בטלוויזיה/רדיו: מ-13.10. הצבעה בנציגויות בחו"ל: 15.10. הסכמי עודפים: עד 16.10. יום אחרון לפרסום סקרים: 23.10. **יום הבחירות: 27.10.2026** (`src/lib/config.ts` → `ELECTION_DATE`). תוצאות רשמיות: 4.11.
- **הרשימות המרכזיות**: ליכוד (נתניהו; תקווה חדשה של סער מתמזגת פנימה; פריימריז נערכו 17.8) · "ביחד" (בנט + לפיד, מאפריל 2026; דועכת ל-6–13 מנדטים) · "ישר!" (איזנקוט; 17–24) · הדמוקרטים (גולן; 8–12) · ישראל ביתנו (ליברמן; 6–8) · ש"ס (דרעי; ~10) · יהדות התורה (גולדקנופף; ~7) · עוצמה יהודית (בן גביר; ~7; סירב סופית לאיחוד עם סמוטריץ') · הציונות הדתית-זהות (סמוטריץ' + פייגלין; סביב אחוז החסימה) · "עמך ישראל" (עופר וינטר, מפלגה חדשה; 0–8 בסקרים) · כחול לבן (גנץ; מתפרקת, מתחת לאחוז החסימה) · הרשימה המשותפת (חד"ש-תע"ל-בל"ד, ג'בארין בראש; רע"ם של עבאס רצה לבד) · "הבית הציוני – המילואימניקים" (הנדל + טרופר).
- **סקרים אחרונים**: ערוץ 14 (פילבר) 30.8: ליכוד 31, ישר! 24, ש"ס 10, קואליציה 63 / אופוזיציה 45 / ערבים 12. כאן 11 19.8: ישר! 24, ליכוד 23. חדשות 13 2.9: גוש נתניהו 53 / גוש איזנקוט 53 / ערבים 14. חדשות 13 26.8: ליכוד 19. ממוצע סקרים (skarim.org): ישר! ~24.
- **מקורות סקרים קבועים**: חדשות 12 (מנו גבע), חדשות 13 (קמיל פוקס), כאן 11 (קנטאר), ערוץ 14 (שלמה פילבר / Next Data), מעריב (לזר, שישי), ישראל היום, i24NEWS (דיירקט פולס).
- **סיפורים פתוחים**: גולן נגד שרה נתניהו (מכתב התראה 1.9 אחרי ריאיון בערוץ 14 ב-31.8; היא ענתה במכתב נגדי) · בקשת החנינה של נתניהו אצל הרצוג (מ-30.11.2025; הרצוג מקדם גישור) · עדות נתניהו במשפטו הסתיימה 24.6.2026 · ועדת חקירה ל-7.10 (היועמ"שית: להשאיר לממשלה הבאה) · עתירות פסילה צפויות נגד בן גביר אחרי הגשת הרשימות · איזנקוט קרא לעימות טלוויזיוני, נתניהו לא השיב · מלחמת איראן (28.2–17.6.2026) הסתיימה במזכר הבנות.
